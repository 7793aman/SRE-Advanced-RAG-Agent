# Data

## `noisy_data 2/` (gitignored — 802 MB, 820 files)

The "enterprise document dump" the RAG pipeline ingests: a deliberately noisy,
heterogeneous pile of technical PDFs and a few HTML files (CUDA, perfect hashing,
storage internals, kernel debugging, databases, etc.). Most of it is irrelevant
to any given query — that noise is the point of the exercise.

Not committed (size + third-party). Keep the folder in place at the repo root.
If it goes missing, restore from the course materials.
